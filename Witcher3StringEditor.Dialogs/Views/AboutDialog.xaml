<Window
    x:Class="Witcher3StringEditor.Dialogs.Views.AboutDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:globalization="clr-namespace:System.Globalization;assembly=mscorlib"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:Witcher3StringEditor.Dialogs.ViewModels"
    Title="{lex:Loc AboutDialogTitle}"
    Width="800"
    Height="500"
    d:DataContext="{d:DesignInstance Type=vm:AboutDialogViewModel}"
    lex:LocalizeDictionary.DesignCulture="en"
    lex:ResxLocalizationProvider.DefaultAssembly="Witcher3StringEditor.Dialogs"
    lex:ResxLocalizationProvider.DefaultDictionary="Strings"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    ResizeMode="NoResize"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ui:FontIcon
            Width="100"
            Margin="24,24,16,0"
            FontSize="50"
            Glyph="&#xE946;" />
        <Grid
            Grid.Row="0"
            Grid.Column="1"
            Margin="0,24,24,0"
            VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock HorizontalAlignment="Right" Text="{lex:Loc Version}" />
            <TextBlock
                Grid.Row="0"
                Grid.Column="2"
                Text="{Binding AboutInfo[Version]}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                HorizontalAlignment="Right"
                Text="{lex:Loc AppBuildTime}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="2"
                Text="{Binding AboutInfo[BuildTime], ConverterCulture={x:Static globalization:CultureInfo.CurrentCulture}}" />
            <TextBlock
                Grid.Row="4"
                Grid.Column="0"
                HorizontalAlignment="Right"
                Text="{lex:Loc OS}" />
            <TextBlock
                Grid.Row="4"
                Grid.Column="2"
                Text="{Binding AboutInfo[OS]}" />
            <TextBlock
                Grid.Row="6"
                Grid.Column="0"
                HorizontalAlignment="Right"
                Text="{lex:Loc Runtime}" />
            <TextBlock
                Grid.Row="6"
                Grid.Column="2"
                Text="{Binding AboutInfo[Runtime]}" />
        </Grid>
        <ScrollViewer
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="16"
            BorderBrush="LightGray"
            BorderThickness="1">
            <ItemsControl ItemsSource="{Binding AboutInfo[Package]}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="16,16,0,0" Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" />
                            <TextBlock Margin="12,0,0,0" Text="{Binding Version}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
